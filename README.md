# What is it CodeFooding
Is a web application build by React.js. the data rendered by fetching an API.

# Requirement packages
   -splidejs/react-splide
   -react-router-dom
   -framer-motion 
   -react-icons
   -styled-components
